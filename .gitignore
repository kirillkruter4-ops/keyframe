node_modules/
out/
dist/
.tmp-mpv/

# mpv.exe — 113 МБ статической сборки. В git ему не место:
# разработчики и CI берут его через `npm run fetch-mpv`.
resources/mpv/

*.tsbuildinfo
*.log
.DS_Store

# Иконка сгенерирована из resources/logo.svg, но лежит в репозитории:
# меняется она раз в год, а иначе CI пришлось бы тянуть sharp ради одного файла.
# Пересобрать: npm run icon

release/
